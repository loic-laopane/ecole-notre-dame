APP_ENV=dev
APP_SECRET=change_me_32_chars_random_string

# ── MySQL ────────────────────────────────────────────────
# Local Docker :
DATABASE_URL="mysql://ndsl_user:ndsl_secret@127.0.0.1:3306/ndsl?serverVersion=8.0&charset=utf8mb4"

# PlanetScale / Railway / Clever Cloud (production) :
# DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DB?serverVersion=8.0&charset=utf8mb4"

# JWT
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=ndsl_jwt_passphrase
JWT_TOKEN_TTL=3600

# OAuth2 – Google
OAUTH_GOOGLE_CLIENT_ID=your_google_client_id
OAUTH_GOOGLE_CLIENT_SECRET=your_google_client_secret

# OAuth2 – Microsoft (optionnel)
OAUTH_MICROSOFT_CLIENT_ID=your_microsoft_client_id
OAUTH_MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret
OAUTH_MICROSOFT_TENANT=common

# CORS – autoriser localhost + Vercel
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$|^https://.*\.vercel\.app$'

# URL du frontend (redirections OAuth2)
FRONTEND_URL=http://localhost:3000
